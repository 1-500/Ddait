import React from 'react';

import CustomInput from '../../../components/CustomInput';

const SetRoomTitle = () => {
  return <CustomInput size="large" placeholder="경쟁방 이름을 입력하세요." theme="primary" />;
};

export default SetRoomTitle;
