import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const hook = () => {
  return (
    <View>
      <Text>hook</Text>
    </View>
  );
};

export default hook;

const styles = StyleSheet.create({});
